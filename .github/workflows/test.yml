name: Failure Handling Demo

on: push

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 (Success)
        run: echo "This will pass"

      - name: Step 2 (Fail intentionally)
        run: exit 1

      - name: Step 3 (Should it run?)
        run: echo "This depends on failure behavior"
